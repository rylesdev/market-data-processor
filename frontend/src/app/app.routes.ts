import { Routes } from '@angular/router';
import { MarketDataComponent } from './market-data/market-data';

export const routes: Routes = [
  {
    path: 'market-data',
    component: MarketDataComponent
  }
];
